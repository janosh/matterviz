{
  "name": "matterviz-wasm",
  "version": "0.0.3",
  "description": "WASM bindings for ferrox structure matching library",
  "type": "module",
  "main": "index.js",
  "types": "types.d.ts",
  "exports": {
    ".": {
      "types": "./types.d.ts",
      "import": "./index.js"
    },
    "./ferrox_bg.wasm": "./pkg/ferrox_bg.wasm",
    "./atomic_scattering_params.json": "./data/atomic_scattering_params.json"
  },
  "files": [
    "index.js",
    "types.d.ts",
    "pkg/",
    "data/"
  ],
  "scripts": {
    "build": "cd ../rust && wasm-pack build --target web --features wasm --no-default-features && cd ../rust-wasm && rm -rf pkg && cp -r ../rust/pkg . && mkdir -p data && cp ../rust/src/atomic_scattering_params.json data/",
    "build:dev": "cd ../rust && wasm-pack build --target web --features wasm --no-default-features --dev && cd ../rust-wasm && rm -rf pkg && cp -r ../rust/pkg . && mkdir -p data && cp ../rust/src/atomic_scattering_params.json data/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/janosh/matterviz.git",
    "directory": "extensions/rust-wasm"
  },
  "keywords": [
    "wasm",
    "crystallography",
    "structure-matching",
    "materials-science"
  ],
  "author": "Janosh Riebesell <janosh.riebesell@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/janosh/matterviz/issues"
  },
  "homepage": "https://github.com/janosh/matterviz#readme"
}
