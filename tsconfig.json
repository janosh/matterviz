{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "strict": true,
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "bundler",
    // To have warnings/errors of the Svelte compiler at the correct position,
    // enable source maps by default.
    "sourceMap": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    // allow importing @wooorm/starry-night/style/both.css (types via its .d.css.ts) from +layout.svelte
    "allowArbitraryExtensions": true,
    "skipLibCheck": true
  },
  // anywidget-renderers.test.ts excluded: it imports the built matterviz package,
  // duplicating Svelte's Snippet type and cascading false errors (mocked at runtime).
  "exclude": ["src/scripts", "extensions", "tests/vitest/anywidget-renderers.test.ts"]
}
